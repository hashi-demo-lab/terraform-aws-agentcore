# Validation Report: terraform-aws-agentcore

| Field    | Value                |
| -------- | -------------------- |
| Branch   | feat/002-agentcore   |
| Date     | 2026-06-09           |
| Provider | hashicorp/aws >= 6.21 (lock 6.49) |

## terraform test

| Test File                  | Result |
| -------------------------- | ------ |
| unit_basic.tftest.hcl      | PASS   |
| unit_complete.tftest.hcl   | PASS   |
| unit_edge_cases.tftest.hcl | PASS   |
| unit_validation.tftest.hcl | PASS   |

**Summary**: 29/29 run blocks passed (1 + 1 + 6 + 21). Acceptance/integration tests in `tests/acceptance/` are environment-gated (require AWS credentials) and intentionally not run.

## terraform validate

**Result**: CLEAN (deprecation warning on `data.aws_region.current.name` resolved during auto-fix)

## terraform fmt -check

**Result**: FORMATTED (no changes needed, recursive)

## tflint

**Result**: CLEAN (no errors; 5 Notice-level advisories only)

- main.tf:38, main.tf:69, security.tf:41, security.tf:195, security.tf:447 — `aws_resource_missing_tags` — Notice: org `.tflint.hcl` rule wants `Environment`/`Application` tags. These are consumer-supplied via `var.tags` (merged onto every resource); the module's required tags `Name` + `ManagedBy` are present. Notice severity, non-blocking, not a module defect.

## pre-commit run --all-files

**Result**: SKIPPED (environment) — hook install fetches over SSH which is blocked in this environment. Equivalent checks (fmt, validate, tflint, trivy, terraform-docs) run directly and pass.

## trivy config

| Metric   | Count |
| -------- | ----- |
| Total    | 0 (terraform) |
| Defects  | 0     |
| Accepted | 0     |

### Defects (block release)

None. Zero CRITICAL and zero HIGH across all targets. The module `.tf` files produce **no** AVD findings.

### Accepted Risks (do not block release)

| AVD-ID  | Severity | File:Line | Description | Justification |
| ------- | -------- | --------- | ----------- | ------------- |
| DS-0001 | MEDIUM   | .devcontainer/*/Dockerfile | `:latest` tag in FROM | Out-of-module tooling Dockerfiles, not module code. Not a Terraform defect. |
| DS-0026 | LOW      | .devcontainer/*/Dockerfile | No HEALTHCHECK | Same — devcontainer images, out of module scope. |

## Security Checklist

Controls from design.md Section 4.

| # | Control                | Result | Evidence |
| - | ---------------------- | ------ | -------- |
| 1 | Encryption at rest     | PASS   | CMK default-on (`create_kms_key=true`, `enable_key_rotation=true`); wired to Gateway `kms_key_arn`, Memory `encryption_key_arn`, token vault `key_type=CustomerManagedKey`, log groups `kms_key_id` (security.tf, main.tf). |
| 2 | Encryption in transit  | PASS   | Platform-enforced HTTPS/TLS by AgentCore; no plaintext transport resource created. |
| 3 | Public access          | PASS   | `network_mode` validated to `PUBLIC` only; gateway auth mandatory (`AWS_IAM`/`CUSTOM_JWT`), no anonymous option; no 0.0.0.0/0 ingress resource. |
| 4 | IAM least privilege    | PASS   | Per-component roles trust only `bedrock-agentcore.amazonaws.com` with `aws:SourceAccount`+`aws:SourceArn` guards; resource-scoped ECR/Logs/Metrics/Bedrock/KMS; only unavoidable `*` (ecr:GetAuthorizationToken, xray, PutMetricData with namespace condition) isolated. No `*:*`. |
| 5 | Logging                | PASS   | `enable_observability=true` creates CMK-encrypted, retention-bounded (default 90d) log groups; never indefinite. |
| 6 | Tagging                | PASS   | `merge(local.module_tags, var.tags)`; defaults `Name` + `ManagedBy="terraform"`; consumer tags win. |
| 7 | Secret handling        | PASS   | Write-only `*_wo` credential args; `api_key_credential_providers`/`oauth2_credential_providers` vars and `*_secret_arns` outputs marked `sensitive=true`; `nonsensitive(keys(...))` used only for for_each addressing, not values. |

## Design Conformance

- Resource inventory: 24/24 resource types from §2 present (incl. `aws_kms_key`, `aws_kms_key_policy`, `aws_kms_alias`, 3 IAM roles + policies, `memory_inference` attachment, 2 log groups, runtime, endpoint, gateway, gateway_target, memory, memory_strategy, token_vault_cmk, api_key/oauth2 credential providers, workload_identity). Singletons use `count`, collections use `for_each`. Explicit `depends_on` to IAM/KMS policy present on each AgentCore resource.
- Variables: 36/36 declared with correct types, defaults, validations; secret-bearing vars `sensitive=true`. (`gateway_targets`, `memory_strategies`, `oauth2_credential_providers` typed `any` — deliberate for deeply nested optional shapes; see findings.)
- Outputs: 20/20 declared; conditionals use `try()`; secret ARNs `sensitive=true`.
- Security controls: 7/7 enforced (table above).
- Test coverage: 4/4 unit scenario groups; acceptance + integration files present.
- File organization: compliant — standard structure, no `provider {}` in root, largest file 458 lines (<500), examples/basic + examples/complete each with provider config.
- Checklist §6: 6/6 items (A–F) complete.

## Quality Score

### Overall: 8.9/10.0 — Excellent

| # | Dimension              | Score | Issues |
| - | ---------------------- | ----- | ------ |
| 1 | Resource Design        | 9.0   | 0 P0, 0 P1 |
| 2 | Security & Compliance  | 9.0   | 0 P0, 0 P1 |
| 3 | Code Quality           | 8.5   | 0 P0, 0 P1, 1 P3 |
| 4 | Variables & Outputs    | 8.5   | 0 P0, 0 P1, 1 P3 |
| 5 | Testing                | 9.0   | 0 P0, 0 P1 |
| 6 | Constitution Alignment | 9.0   | 0 P0, 0 P1 |

**Production Readiness: Ready** (D2 Security = 9.0, well above 5.0 floor)

### Top Issues

| # | Severity | Dimension | File:Line | Issue | Remediation |
| - | -------- | --------- | --------- | ----- | ----------- |
| 1 | P3 | Variables | variables.tf:208,235,296 | `gateway_targets`, `memory_strategies`, `oauth2_credential_providers` typed `any` rather than explicit `map(object({...}))` with `optional()` | Tighten to explicit optional-attribute object types for stronger plan-time validation. Deliberate tradeoff for deeply-nested polymorphic shapes; acceptable. |
| 2 | P3 | Code Quality | examples/ | examples could add a remote-state/backend note for production consumers | Optional doc polish only. |

## Auto-Fixes Applied

- Resolved `data.aws_region.current.name` deprecation → `.region` (security.tf:13), and updated the 4 `mock_data "aws_region"` blocks (tests/unit_basic, unit_complete, unit_edge_cases, unit_validation) to supply `region`. Re-ran fmt (clean), validate (now CLEAN, no warning), test (29/29 still pass).

## Issues Requiring Manual Fix

None blocking. The two P3 items above are optional refinements.

## Overall Status

**PASS** — PR-ready. fmt clean, validate clean, 29/29 unit tests pass, tflint no errors, trivy 0 CRITICAL/0 HIGH (0 terraform findings), terraform-docs current, all 7 security controls pass.
